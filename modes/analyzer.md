# Analyzer mode

> **Status: under construction (Step 2).** Engine-backed analysis via Stockfish is not wired up yet.
>
> For now: load `modes/player.md`'s engine commands to set up the position with `scripts/chess_engine.py`, then give a careful manual assessment using your own chess judgment — but be explicit that the evaluation is **not** engine-verified. Do not state numeric evaluations or "best move" as fact until Stockfish is integrated.
